const inputs = document.querySelector(".users");
let arrs = [1, 2, 3, 4, 5, 6];

for (let index = 0; index < array.length; index++) {
  const element = array[index];
}

let arrPlus = arrs.map((arr) => arr++);
